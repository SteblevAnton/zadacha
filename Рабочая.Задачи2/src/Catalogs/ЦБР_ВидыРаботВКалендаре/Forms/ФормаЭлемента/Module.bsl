&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Элемент = РеквизитФормыВЗначение("Объект");
	Цвет = Элемент.ЦветЗаписи.Получить();
	ЦветЗаписи = Цвет;
КонецПроцедуры

&НаСервере
Процедура ПередЗаписьюНаСервере(Отказ, ТекущийОбъект, ПараметрыЗаписи)
	Хранилище = Новый ХранилищеЗначения(ЦветЗаписи);
	ТекущийОбъект.ЦветЗаписи = Хранилище;
КонецПроцедуры
